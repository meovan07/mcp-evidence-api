{
  "name": "mcp-evidence-api",
  "version": "0.1.0",
  "description": "MCP server that drives backend/API requests and packages request+response pairs as evidence an endpoint works.",
  "private": true,
  "type": "module",
  "bin": {
    "mcp-evidence-api": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare": "npm run build",
    "start": "node dist/index.js",
    "dev": "tsc -p tsconfig.json --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "imapflow": "^1.4.7",
    "mailparser": "^3.9.14",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/mailparser": "^3.4.6",
    "@types/node": "^22.10.5",
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": ">=20"
  }
}
